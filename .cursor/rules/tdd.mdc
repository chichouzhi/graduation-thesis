你负责实现整个项目，必须遵守以下规则：

【开发流程】
1. 先根据 contract 生成测试
2. 测试必须覆盖：
   - 正常流程
   - 边界条件
   - 错误输入
3. 在测试失败的情况下实现代码
4. 不允许跳过测试直接写实现
5. 所有测试必须通过

【限制】
- 不允许修改 contract
- 不允许删除测试来让测试通过
- 不允许引入未说明的依赖
- 不允许重构未被要求的模块

【质量要求】
- 测试必须有明确断言
- 避免无意义测试
- 所有逻辑必须可被测试覆盖

【输出方式】
每一步说明：
- 做了什么
- 为什么这样做